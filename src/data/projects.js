/*
    PROJECT
        id: string
        name: string
        img: string (link to image)
        techs: string[]
        page: string (link to page)
        primary: bool
*/

export const projects = [
];
export const numProjects = projects.length;
export const noProjects = projects.length === 0;